spring:
  application:
    name: poomasi
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        enable_lazy_load_no_trans: true
        hbm2ddl:
          jdbc_metadata_extraction_strategy: individually
  datasource:
    url: jdbc:mysql://localhost:3306/poomasi
    username: root
    password: 12345678

logging:
  level:
    org:
      springframework:
        web: DEBUG
        security: TRACE

kakao :
  login :
    REST-API-KEY :
      {입력해야함}
    REDIRECT-URI :
      {입력해야함}
jwt:
  secret: poomasipoomasipoomasipoomasipoomasipoomasi
  expiration: 86400
